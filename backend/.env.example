# Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=development

# Server Port
PORT=5000

# MongoDB Connection String
# For production, use your MongoDB Atlas connection string
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database_name

# JWT Authentication
# IMPORTANT: Change this to a strong random string in production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=30d

# Production Deployment Notes:
# 1. Never commit .env files to Git
# 2. Set these variables in your hosting platform (Render, Vercel, etc.)
# 3. Use a strong, randomly generated JWT_SECRET in production
# 4. Ensure your MongoDB Atlas IP whitelist includes your hosting provider
